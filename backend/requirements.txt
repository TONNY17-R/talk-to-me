# Render-safe dependency set
# This file is used by Render's default build command.
# It intentionally points to the lightweight dependency list to avoid
# the TensorFlow/PyTorch install failures on the default Python runtime.

-r requirements_minimal.txt
flask-jwt-extended==4.5.2
